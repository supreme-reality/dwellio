spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

dwellio:
  auth0:
    audience: https://api.dwellio.local
  razorpay:
    webhook-secret: test-razorpay-webhook-secret
    allow-stub: true
  storage:
    type: memory
    bucket: dwellio-documents-test
